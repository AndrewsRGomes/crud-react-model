

const isAuthenticated = () => true;

export default isAuthenticated
//aqui ficará a função que verifica a autenticação do usuario
//vai ser criado um token no back-end se o usuario estiver autenticado
// ele vai salvar no localStorage o token
// e verificar a cada requisição se o usuario está autenticado